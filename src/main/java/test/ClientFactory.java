package test;

/**
 Fabrique de clients
 */
public class ClientFactory {
/*
	protected String replaceAccent(String aString) {
		aString = aString.replace((char)224', 'a');
		aString = aString.replace((char)226', 'a');
		aString = aString.replace((char)228', 'a');
		aString = aString.replace((char)233', 'e');
		aString = aString.replace((char)232', 'e');
		aString = aString.replace((char)234', 'e');
		aString = aString.replace((char)235', 'e');
		aString = aString.replace((char)238', 'i');
		aString = aString.replace((char)239', 'i');
		aString = aString.replace((char)244', 'o');
		aString = aString.replace((char)246', 'o');
		aString = aString.replace((char)249', 'u');
		aString = aString.replace((char)251', 'u');
		aString = aString.replace((char)252', 'u');
		aString = aString.replace('-', ' ');
		aString = aString.replace('\'', ' ');
		return aString;
	}
	
	protected java.sql.Date utilToSqlDate(java.util.Date utilDate) {
		if (utilDate == null)
			return null;
		java.sql.Date sqlDate = new java.sql.Date(utilDate.getTime());
		return sqlDate;
	}
	
	protected java.util.Date sqlToUtilDate(java.sql.Date sqlDate) {
		if (sqlDate == null)
			return null;
		java.util.Date utilDate = new java.util.Date(sqlDate.getTime());
		return utilDate;
	}
	*/
}